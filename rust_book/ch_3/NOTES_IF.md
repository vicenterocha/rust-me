# if statements

```rust
fn main() {
    let condition = true;
    // using the if statement directly in variable assignement
    let number = if condition { 5 } else { 6 };

    println!("The value of number is: {number}");
}
```
